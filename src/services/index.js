// Define any external services that your API uses here
// We recommend splitting functions among all of the
// services you use. For example, for a service
// that stores specific data, you could do:
// 
// export * as dataService from './dataService';
// 
// And inside the file do something like:
//
// export const getAll = async () => {
//   const response = await axios.get(`${DATA_URL}/datas`);
//   const results = response.data;
//   return results;
// };
